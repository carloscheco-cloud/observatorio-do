import {ApiState,date,ExecutiveNav} from "@/components/executive";import {executive,ExecutiveApiError} from "@/lib/executive-api";type SP=Record<string,string|string[]|undefined>;const one=(v:string|string[]|undefined)=>Array.isArray(v)?v[0]:v;
export const metadata={title:"Cambios recientes del Poder Ejecutivo",description:"Cambios institucionales persistidos y verificables."};
export default async function Changes({searchParams}:{searchParams:Promise<SP>}){const sp=await searchParams,p=new URLSearchParams();for(const k of ["date_from","date_to","change_type","institution_slug","page"]){const v=one(sp[k]);if(v)p.set(k,v)}try{const data=await executive.changes(p);return <div className="shell section"><ExecutiveNav/><h1>Cambios recientes</h1><p>Eventos persistidos; no son noticias ni interpretaciones políticas.</p><form className="filters"><label>Desde<input type="date" name="date_from" defaultValue={one(sp.date_from)}/></label><label>Hasta<input type="date" name="date_to" defaultValue={one(sp.date_to)}/></label><label>Tipo<select name="change_type" defaultValue={one(sp.change_type)??""}><option value="">Todos</option><option value="appointment">Nombramiento</option><option value="termination">Terminación</option><option value="new_relationship">Relación</option><option value="new_assessment">Evaluación</option><option value="methodology_change">Metodología</option></select></label><label>Institución (slug)<input name="institution_slug" defaultValue={one(sp.institution_slug)}/></label><button className="button">Aplicar filtros</button></form>{data.items.length?<ol className="timeline">{data.items.map(c=><li key={c.id}><time dateTime={c.occurred_at}>{date(c.occurred_at)}</time><h2>{c.description}</h2><p>{c.institution?.official_name??"Alcance general"} · {c.change_type}</p></li>)}</ol>:<p className="empty">No se localizaron cambios estructurados para los filtros seleccionados.</p>}</div>}catch(e){return <div className="shell section"><h1>Cambios recientes</h1><ApiState message={e instanceof ExecutiveApiError?e.message:"API no disponible"}/></div>}}
