import type {Metadata} from "next"; import Link from "next/link"; import {ApiState,date,ExecutiveNav} from "@/components/executive"; import {executive,ExecutiveApiError} from "@/lib/executive-api";
export const metadata:Metadata={title:"Autoridades del Poder Ejecutivo",description:"Autoridades actuales confirmadas y actos de designación localizados.",alternates:{canonical:"/poder-ejecutivo/autoridades"}}; type SP=Record<string,string|string[]|undefined>;const one=(v:string|string[]|undefined)=>Array.isArray(v)?v[0]:v;
export default async function Authorities({searchParams}:{searchParams:Promise<SP>}){const sp=await searchParams,p=new URLSearchParams();for(const k of ["search","institution_slug","position_type","active_only","page"]){const v=one(sp[k]);if(v)p.set(k,v)}p.set("page_size","12");try{const data=await executive.authorities(p);return <div className="shell section"><ExecutiveNav/><h1>Autoridades</h1><p>Nombramientos públicos confirmados dentro de las fuentes incorporadas.</p><form className="filters"><label>Buscar<input name="search" defaultValue={one(sp.search)}/></label><label>Institución (slug)<input name="institution_slug" defaultValue={one(sp.institution_slug)}/></label><label>Cargo<input name="position_type" defaultValue={one(sp.position_type)}/></label><label className="check"><input type="checkbox" name="active_only" value="true" defaultChecked={one(sp.active_only)!=="false"}/> Solo activos</label><button className="button">Aplicar filtros</button></form>{data.items.length?<div className="directory">{data.items.map(a=><article className="card" key={a.appointment_id}><p className="eyebrow">{a.institution.official_name}</p><h2>{a.public_name}</h2><p>{a.position}</p><dl><dt>Inicio</dt><dd>{date(a.start_date)}</dd><dt>Nombramiento</dt><dd>{a.appointment_status}</dd><dt>Acto</dt><dd>{a.appointment_act_status}</dd><dt>Verificación</dt><dd>{a.verification_status}</dd></dl><Link href={`/poder-ejecutivo/autoridades/${a.appointment_id}`}>Ver detalle</Link></article>)}</div>:<p className="empty">No se localizaron datos estructurados para esta sección en las fuentes actualmente incorporadas.</p>}<nav className="pagination" aria-label="Paginación"><span>Página {data.page} de {Math.max(data.pages,1)}</span>{data.page>1&&<Link href={{query:{...sp,page:data.page-1}}}>Anterior</Link>}{data.page<data.pages&&<Link href={{query:{...sp,page:data.page+1}}}>Siguiente</Link>}</nav></div>}catch(e){return <div className="shell section"><h1>Autoridades</h1><ApiState message={e instanceof ExecutiveApiError?e.message:"API no disponible"}/></div>}}
