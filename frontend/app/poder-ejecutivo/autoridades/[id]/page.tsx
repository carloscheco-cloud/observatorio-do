import {ApiState,date,EvidenceLink,ExecutiveNav} from "@/components/executive"; import {executive,ExecutiveApiError} from "@/lib/executive-api"; import AuthorityNotFound from "./not-found";
export default async function Authority({params}:{params:Promise<{id:string}>}){try{const a=await executive.authorityDetail((await params).id);return <div className="shell section"><ExecutiveNav/><h1>{a.public_name}</h1><p>{a.positions.join(" · ")}</p>{a.appointments.map(x=><section className="card" key={x.appointment_id}><h2>{x.position}</h2><p>{x.institution.official_name}</p><p>{date(x.start_date)} – {x.end_date?date(x.end_date):"Actual"}</p><p>{x.appointment_status} · {x.verification_level}</p>{x.appointment_evidence.map(e=><p key={e.id}><EvidenceLink evidence={e}/></p>)}</section>)}{a.limitations.length>0&&<section className="notice"><h2>Limitaciones</h2><ul>{a.limitations.map(x=><li key={x}>{x}</li>)}</ul></section>}</div>}catch(e){if(e instanceof ExecutiveApiError&&e.kind==="not_found")return <AuthorityNotFound/>;return <div className="shell section"><ApiState message={e instanceof Error?e.message:"API no disponible"}/></div>}}
